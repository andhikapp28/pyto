---
// Animasi #4 (plan/bab-16-animasi.md), Centerpiece 2 — roda alfabet + cincin
// luar berputar 3 langkah untuk menyandikan "AGEN RAHASIA" jadi
// "DJHQ UDKDVLD" huruf demi huruf. Spasi dilewati tanpa memutar apa pun
// (menunjukkan cabang `else`). Sekali jalan saat terlihat, boleh diulang
// lewat hover/klik/keyboard.
import AlphabetWheel from './AlphabetWheel.astro';
import CodeBlock from './CodeBlock.astro';

const SRC = 'AGEN RAHASIA';
const SHIFT = 3;

function shiftChar(ch: string, shift: number) {
  if (ch >= 'A' && ch <= 'Z') {
    const code = ch.charCodeAt(0) - 65;
    const newCode = (((code + shift) % 26) + 26) % 26;
    return String.fromCharCode(newCode + 65);
  }
  return ch;
}

const srcChars = SRC.split('');
const outChars = srcChars.map((c) => shiftChar(c, SHIFT));

const code = `def sandikan(pesan, geseran):
    hasil = ""
    for huruf in pesan:
        if huruf >= "A" and huruf <= "Z":
            angka = ord(huruf) - ord("A")
            angka_baru = (angka + geseran) % 26
            hasil += chr(angka_baru + ord("A"))
        else:
            hasil += huruf
    return hasil

pesan_sandi = sandikan("AGEN RAHASIA", 3)
print(pesan_sandi)   # DJHQ UDKDVLD`;
---

<div
  class="csd-demo card"
  data-anim="csd-demo"
  tabindex="0"
  role="button"
  aria-label="Ulangi animasi sandi Caesar menggeser pesan AGEN RAHASIA"
>
  <CodeBlock {code} />

  <div class="csd-demo__stage">
    <AlphabetWheel idPrefix="csd" size={196} showRing needleIndex={null} label="Roda + Cincin Geser (3)" />

    <div class="csd-demo__strings">
      <div class="csd-demo__row">
        <span class="csd-demo__row-label">Pesan asli</span>
        <div class="csd-demo__chars">
          {
            srcChars.map((c, i) => (
              <span class="csd-demo__char" id={`csd-src-${i}`}>
                {c === ' ' ? ' ' : c}
              </span>
            ))
          }
        </div>
      </div>
      <div class="csd-demo__row">
        <span class="csd-demo__row-label">Pesan tersandi</span>
        <div class="csd-demo__chars">
          {
            outChars.map((c, i) => (
              <span class="csd-demo__char csd-demo__char--out" id={`csd-out-${i}`}>
                {c === ' ' ? ' ' : c}
              </span>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</div>

<p class="csd-demo__hint">
  💡 Cincin berputar sekali sejauh 3 langkah, jarum menunjuk tiap huruf asli, huruf hasil menyala
  di posisi barunya. <em>(Klik / hover untuk ulangi.)</em>
</p>

<style>
  .csd-demo {
    display: flex;
    flex-direction: column;
    gap: 18px;
    padding: 22px 24px;
    cursor: pointer;
  }

  .csd-demo__stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
  }

  .csd-demo__strings {
    display: flex;
    flex-direction: column;
    gap: 10px;
    width: 100%;
  }

  .csd-demo__row {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .csd-demo__row-label {
    flex-shrink: 0;
    width: 108px;
    font-size: 12px;
    font-weight: 800;
    color: var(--slate);
    text-transform: uppercase;
    letter-spacing: 0.3px;
  }

  .csd-demo__chars {
    display: flex;
    gap: 3px;
    flex-wrap: wrap;
  }

  .csd-demo__char {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 22px;
    height: 26px;
    border-radius: 6px;
    font-family: var(--font-code);
    font-weight: 700;
    font-size: 14px;
    color: var(--ink);
    background: var(--cloud);
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .csd-demo__char.is-current {
    background: var(--pyto-green);
    color: #fff;
  }

  .csd-demo__char--out {
    opacity: 0.25;
  }

  .csd-demo__char--out.is-in {
    opacity: 1;
    background: rgba(139, 111, 224, 0.16);
    color: var(--grape);
  }

  .csd-demo__hint {
    font-size: 14px;
    line-height: 1.55;
    margin: 0 2px;
    color: var(--slate);
  }

  @media (max-width: 600px) {
    .csd-demo__row-label {
      width: 100%;
    }
  }
</style>

<script>
  import { observeOnce, wireReplay } from '../scripts/reveal-anim.js';

  const SRC = 'AGEN RAHASIA';
  const SHIFT = 3;
  const STEP_ANGLE = 360 / 26;

  function shiftChar(ch: string, shift: number) {
    if (ch >= 'A' && ch <= 'Z') {
      const code = ch.charCodeAt(0) - 65;
      const newCode = (((code + shift) % 26) + 26) % 26;
      return String.fromCharCode(newCode + 65);
    }
    return ch;
  }

  function resetWheel() {
    for (let i = 0; i < 26; i += 1) {
      document.getElementById(`csd-letter-${i}`)?.classList.remove('is-active', 'is-target');
    }
  }

  function play() {
    const ring = document.getElementById('csd-ring');
    const needle = document.getElementById('csd-needle');
    if (!ring || !needle) return;

    resetWheel();
    ring.style.transform = 'translate(-50%, -50%) rotate(0deg)';
    needle.style.opacity = '0';

    for (let i = 0; i < SRC.length; i += 1) {
      document.getElementById(`csd-src-${i}`)?.classList.remove('is-current');
      document.getElementById(`csd-out-${i}`)?.classList.remove('is-in');
    }

    setTimeout(() => {
      ring.style.transform = `translate(-50%, -50%) rotate(${STEP_ANGLE * SHIFT}deg)`;
    }, 150);

    const STEP_DELAY = 230;
    let t = 700;

    SRC.split('').forEach((ch, i) => {
      setTimeout(() => {
        document.getElementById(`csd-src-${i}`)?.classList.add('is-current');

        if (ch >= 'A' && ch <= 'Z') {
          const srcIdx = ch.charCodeAt(0) - 65;
          const outCh = shiftChar(ch, SHIFT);
          const tgtIdx = outCh.charCodeAt(0) - 65;

          resetWheel();
          needle.style.opacity = '1';
          needle.style.transform = `translate(-50%, -100%) rotate(${STEP_ANGLE * srcIdx}deg)`;
          document.getElementById(`csd-letter-${srcIdx}`)?.classList.add('is-active');
          setTimeout(() => {
            document.getElementById(`csd-letter-${tgtIdx}`)?.classList.add('is-target');
          }, 90);
        }

        document.getElementById(`csd-out-${i}`)?.classList.add('is-in');
      }, t);
      t += STEP_DELAY;
    });
  }

  const els = document.querySelectorAll('[data-anim="csd-demo"]');
  observeOnce(els, (el) => {
    el.classList.add('is-in');
    play();
  });

  els.forEach((el) => {
    wireReplay(el, 'is-in');
    el.addEventListener('mouseenter', play);
    el.addEventListener('click', play);
    el.addEventListener('keydown', (e) => {
      if ((e as KeyboardEvent).key === 'Enter' || (e as KeyboardEvent).key === ' ') play();
    });
  });
</script>
